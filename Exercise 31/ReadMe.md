# JS Exercise 31: HTTP GET Request

This project demonstrates how to make an HTTP GET request using JavaScript.

* Uses `fetch()` to get users from an API.
* Checks whether the response is successful.
* Converts the response to JSON.
* Handles errors using `try...catch`.

## Run

Open `index.html`, press `F12`, and check the Console.
