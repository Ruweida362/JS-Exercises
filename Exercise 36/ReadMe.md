# JS Exercise 36: Color Picker

This project demonstrates how to create an interactive color picker using JavaScript.

* Changes the preview background color.
* Saves selected colors in the color history.
* Uses `addEventListener()` to detect color changes.
* Clears the color history using a button.

## Run

Open `index.html`, choose a color, and view the color history.
