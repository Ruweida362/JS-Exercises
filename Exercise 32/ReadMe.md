# JS Exercise 32: Selecting DOM Elements

This project demonstrates how to select HTML elements using JavaScript.

* `querySelector()` 
* `querySelectorAll()`
* Elements are selected using IDs and class names.
* Selected elements are displayed in the browser console.

## Run

Open `index.html`, press `F12`, and check the Console.
