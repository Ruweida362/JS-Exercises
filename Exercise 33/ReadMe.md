# JS Exercise 33: Changing DOM Content

This project demonstrates how to change webpage content using JavaScript.

* `textContent` changes plain text.
* `innerHTML` changes HTML content.
* The button calls the `changeContent()` function.
* Updated content appears after clicking the button.

## Run

Open `index.html` and click the **Change Content** button.
