# JS  30: Callbacks

This project demonstrates how callback functions work in JavaScript.

* `operate()` receives two numbers and a callback.
* The callback performs addition, subtraction, multiplication, or division.
* Results are displayed in the browser console.

## Run

Open `index.html`, press `F12`, and check the Console.
