# JS  Exercise 29: JSON Fetch

This project demonstrates how to fetch JSON data asynchronously using JavaScript.

- Fetches data from a local data.json file.
- Fetches user data from an external API.
- Converts the response using response.json().
- Displays the data in the browser console.


**Run**

Open the project using Live Server, press F12, and check the Console
