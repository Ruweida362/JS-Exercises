# JS Exercise 35: Change Image Style

This project demonstrates how to change an image and its style using JavaScript.

* Changes the image URL using `setAttribute()`.
* Changes border color, width, height, and border radius.
* Uses `prompt()` to collect user input.
* Applies styles using the `style` property.

## Run

Open `index.html` and click the **Change Image** button.
